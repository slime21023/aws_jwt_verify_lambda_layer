{
  "name": "aws_jwt_verify_lambda_layer",
  "version": "1.0.0",
  "description": "The aws lambda layer for aws lambda authorizer by aws-jwt-verify",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dist": "mkdir nodejs && npm install --install-links --prefix nodejs",
    "zip": "zip -r nodejs.zip nodejs",
    "clear": "rm -rf nodejs nodejs.zip"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-jwt-verify": "^4.0.0"
  }
}
